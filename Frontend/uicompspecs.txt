## UI COMPONENT SPECIFICATIONS

1. INPUT REFERENCE (FROM AGENT-1)

Page Name:
KanbanBoardPage

Mapped Layout:
- Header (top bar with search, notifications, avatar)
- Sidebar (left, branding, navigation, footer)
- Main Container (Kanban board with three columns: To Do, In Progress, Done)
- Modal overlays (task creation/edit)
- Snackbar (error/success)
- Popup (confirmation, guided setup)

Components from A1:
- HeaderComponent
- SidebarComponent
- KanbanBoardComponent
- KanbanColumnComponent
- KanbanCardComponent
- AddTaskButtonComponent
- ModalComponent
- SnackbarComponent
- PopupComponent

2. GLOBAL ARCHITECTURE SUMMARY

Layout Type:
- Grid (Kanban columns)
- Flex (Header, Sidebar, Card content)

Main Sections:
- Header
- Sidebar
- Main Content (Kanban Board)
- Modals
- Snackbar
- Popup

Component Strategy:
Reused Components:
- Header
- Sidebar
- Card
- Button
- Modal
- Snackbar
- Grid
- Divider
- Popup

New Components (if any):
- KanbanBoardComponent
- KanbanColumnComponent
- KanbanCardComponent
- AddTaskButtonComponent

3. FOLDER STRUCTURE

src/
 ├── components/
 │    ├── layout/
 │    │    ├── HeaderComponent
 │    │    ├── SidebarComponent
 │    │
 │    ├── feature/
 │    │    ├── KanbanBoardComponent
 │    │    ├── KanbanColumnComponent
 │    │    ├── KanbanCardComponent
 │    │    ├── AddTaskButtonComponent
 │    │
 │    ├── shared/
 │         ├── Button
 │         ├── Card
 │         ├── Modal
 │         ├── Snackbar
 │         ├── Popup
 │         ├── Divider
 │
 ├── pages/
 │    ├── KanbanBoardPage/
 │         ├── KanbanBoardPage.tsx

4. COMPONENT IMPLEMENTATION (REPEATS FOR EACH COMPONENT)

### 4.1 HeaderComponent

Component Type:
Layout

Mapped From A1:
HeaderComponent

Purpose:
Application header with search, notifications, and user avatar

Parent:
KanbanBoardPage

Children:
- SearchBar (from library)
- NotificationIcon (from library)
- Avatar (from library)

Dependencies:
- Search-Bar.pdf
- Icon.pdf
- Avatar.pdf

Library Components Used:
- Header → container
- SearchBar → search input
- Icon → notification
- Avatar → user

TypeScript Specification
Inputs:
- searchText: string
- user: User
Outputs:
- onSearchChange(search: string)
- onNotificationClick()
State:
- activeTab: string
Methods:
- handleSearchChange()
- handleNotificationClick()

HTML STRUCTURE (PSEUDO CODE ONLY)
<Header>
  <SearchBar value={searchText} onChange={onSearchChange} />
  <Icon name="notification" onClick={onNotificationClick} />
  <Avatar user={user} />
</Header>

CSS SPECIFICATION
Layout:
- display: flex; align-items: center; justify-content: space-between;
Spacing:
- padding: 16px; gap: 24px;
Component Styles:
- .header { background: #fff; box-shadow: 0 1px 4px rgba(0,0,0,0.04); }
Responsive:
- @media (max-width: 768px) { flex-direction: column; gap: 12px; }

API INTEGRATION
- None

ERROR HANDLING
- None

INTERACTION FLOW
User types in search → onSearchChange → parent handles filter
User clicks notification → onNotificationClick → show notifications

---

### 4.2 SidebarComponent

Component Type:
Layout

Mapped From A1:
SidebarComponent

Purpose:
Navigation for Kanban scenarios, branding, footer

Parent:
KanbanBoardPage

Children:
- Menu (from library)
- Branding (custom)
- Footer (custom)

Dependencies:
- Sidebar.pdf
- Menu.pdf

Library Components Used:
- Sidebar → container
- Menu → navigation

TypeScript Specification
Inputs:
- menuItems: MenuItem[]
- selected: string
Outputs:
- onMenuSelect(item: string)
State:
- isCollapsed: boolean
Methods:
- handleMenuSelect()
- toggleCollapse()

HTML STRUCTURE (PSEUDO CODE ONLY)
<Sidebar>
  <Branding />
  <Menu items={menuItems} selected={selected} onSelect={onMenuSelect} />
  <Footer />
</Sidebar>

CSS SPECIFICATION
Layout:
- display: flex; flex-direction: column; height: 100vh;
Spacing:
- padding: 16px; gap: 16px;
Component Styles:
- .sidebar { background: #f7f7f9; border-right: 1px solid #e0e0e0; }
Responsive:
- @media (max-width: 1024px) { width: 56px; }

API INTEGRATION
- None

ERROR HANDLING
- None

INTERACTION FLOW
User clicks menu item → onMenuSelect → parent updates selected

---

### 4.3 KanbanBoardComponent

Component Type:
Feature

Mapped From A1:
KanbanBoardComponent

Purpose:
Container for Kanban columns, manages board state

Parent:
MainContainer

Children:
- KanbanColumnComponent (To Do)
- KanbanColumnComponent (In Progress)
- KanbanColumnComponent (Done)

Dependencies:
- Grid.pdf
- Divider.pdf

Library Components Used:
- Grid → layout
- Divider → between columns

TypeScript Specification
Inputs:
- columns: KanbanColumn[]
Outputs:
- onAddTask(columnId: string)
- onCardMove(cardId: string, fromColumn: string, toColumn: string)
State:
- error: string | null
Methods:
- handleAddTask()
- handleCardMove()

HTML STRUCTURE (PSEUDO CODE ONLY)
<Grid columns={3}>
  <KanbanColumnComponent column={columns[0]} />
  <Divider vertical />
  <KanbanColumnComponent column={columns[1]} />
  <Divider vertical />
  <KanbanColumnComponent column={columns[2]} />
</Grid>
<If error>
  <SnackbarComponent message={error} type="error" />
</If>

CSS SPECIFICATION
Layout:
- display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px;
Spacing:
- margin: 0 24px;
Component Styles:
- .kanban-board { min-height: 60vh; }
Responsive:
- @media (max-width: 1024px) { grid-template-columns: 1fr; }

API INTEGRATION
- fetchBoardData() → GET /api/youth-accounts/{youthAccountId}/dashboard

ERROR HANDLING
- If fetch fails → set error → show SnackbarComponent

INTERACTION FLOW
User drags card → handleCardMove → update state
User clicks Add Task → handleAddTask → open ModalComponent

---

### 4.4 KanbanColumnComponent

Component Type:
Feature

Mapped From A1:
KanbanColumnComponent

Purpose:
Represents a Kanban column (To Do, In Progress, Done)

Parent:
KanbanBoardComponent

Children:
- KanbanCardComponent (n)
- AddTaskButtonComponent

Dependencies:
- Card.pdf
- Button.pdf

Library Components Used:
- Card → for each KanbanCardComponent
- Button → AddTaskButtonComponent

TypeScript Specification
Inputs:
- column: KanbanColumn
Outputs:
- onAddTask()
- onCardDrop(cardId: string)
State:
- cards: KanbanCard[]
Methods:
- handleAddTask()
- handleCardDrop()

HTML STRUCTURE (PSEUDO CODE ONLY)
<ColumnContainer aria-label={column.title} aria-labelledby={column.id}-header>
  <ColumnHeader id={column.id}-header>
    <Title>{column.title}</Title>
    <Summary>{column.summary}</Summary>
    <Timestamp>{column.timestamp}</Timestamp>
    <AddTaskButtonComponent columnId={column.id} />
  </ColumnHeader>
  <CardList>
    <For card in column.cards>
      <KanbanCardComponent card={card} />
    </For>
  </CardList>
</ColumnContainer>

CSS SPECIFICATION
Layout:
- display: flex; flex-direction: column; min-width: 0;
Spacing:
- padding: 16px; gap: 12px;
Component Styles:
- .kanban-column { background: #fafbfc; border-radius: 8px; }
Responsive:
- @media (max-width: 768px) { min-width: 100%; }

API INTEGRATION
- None

ERROR HANDLING
- If cards load fails → show error in PopupComponent

INTERACTION FLOW
User clicks Add Task → onAddTask → parent opens modal
User drops card → onCardDrop → parent updates state

---

### 4.5 KanbanCardComponent

Component Type:
Feature

Mapped From A1:
KanbanCardComponent

Purpose:
Displays a single Kanban card/task

Parent:
KanbanColumnComponent

Children:
- None

Dependencies:
- Card.pdf
- Avatar.pdf
- Icon.pdf

Library Components Used:
- Card → container
- Avatar → assignee
- Icon → due date

TypeScript Specification
Inputs:
- card: KanbanCard
Outputs:
- onMove(toColumn: string)
- onEdit()
State:
- isSelected: boolean
Methods:
- handleMove()
- handleEdit()

HTML STRUCTURE (PSEUDO CODE ONLY)
<Card selected={isSelected}>
  <Title>{card.title}</Title>
  <Description>{card.description}</Description>
  <Meta>
    <Avatar user={card.assignee} />
    <Icon name="calendar" /> {card.dueDate}
  </Meta>
  <Actions>
    <Button icon="edit" onClick={onEdit} />
    <Button icon="move" onClick={() => onMove(nextColumn)} />
  </Actions>
</Card>

CSS SPECIFICATION
Layout:
- display: flex; flex-direction: column;
Spacing:
- padding: 12px; gap: 8px;
Component Styles:
- .kanban-card.selected { border: 2px solid #1976d2; }
Responsive:
- @media (max-width: 768px) { font-size: 14px; }

API INTEGRATION
- None

ERROR HANDLING
- If card data invalid → show error in SnackbarComponent

INTERACTION FLOW
User clicks edit → onEdit → open ModalComponent
User clicks move → onMove → parent updates column

---

### 4.6 AddTaskButtonComponent

Component Type:
Feature

Mapped From A1:
AddTaskButtonComponent

Purpose:
Button to trigger task creation modal

Parent:
KanbanColumnComponent

Children:
- None

Dependencies:
- Button.pdf

Library Components Used:
- Button

TypeScript Specification
Inputs:
- columnId: string
Outputs:
- onClick()
State:
- None
Methods:
- handleClick()

HTML STRUCTURE (PSEUDO CODE ONLY)
<Button icon="add" onClick={onClick}>Add Task</Button>

CSS SPECIFICATION
Layout:
- align-self: flex-end;
Spacing:
- margin-top: 8px;
Component Styles:
- .add-task-btn { background: #1976d2; color: #fff; }
Responsive:
- @media (max-width: 768px) { width: 100%; }

API INTEGRATION
- None

ERROR HANDLING
- None

INTERACTION FLOW
User clicks → onClick → parent opens ModalComponent

---

### 4.7 ModalComponent

Component Type:
Shared

Mapped From A1:
ModalComponent

Purpose:
Modal overlay for task creation/edit

Parent:
KanbanBoardPage

Children:
- Form fields (Textbox, Textarea, Date-Picker, etc.)
- Button (Save, Cancel)

Dependencies:
- Modal.pdf
- Textbox.pdf
- Textarea.pdf
- Date-Picker.pdf
- Button.pdf

Library Components Used:
- Modal
- Textbox
- Textarea
- Date-Picker
- Button

TypeScript Specification
Inputs:
- isOpen: boolean
- mode: 'create' | 'edit'
- card?: KanbanCard
Outputs:
- onSubmit(card: KanbanCard)
- onCancel()
State:
- form: { title: string; description: string; dueDate: string; assignee: User }
Methods:
- handleSubmit()
- handleCancel()

HTML STRUCTURE (PSEUDO CODE ONLY)
<Modal open={isOpen}>
  <Form>
    <Textbox label="Title" value={form.title} />
    <Textarea label="Description" value={form.description} />
    <Date-Picker label="Due Date" value={form.dueDate} />
    <Button onClick={onSubmit}>Save</Button>
    <Button onClick={onCancel}>Cancel</Button>
  </Form>
</Modal>

CSS SPECIFICATION
Layout:
- display: flex; flex-direction: column; gap: 16px;
Spacing:
- padding: 24px;
Component Styles:
- .modal-form { width: 400px; max-width: 90vw; }
Responsive:
- @media (max-width: 768px) { width: 100%; }

API INTEGRATION
- onSubmit → (No direct API, recommend POST /tasks)

ERROR HANDLING
- If form invalid → show error in SnackbarComponent

INTERACTION FLOW
User fills form → clicks Save → onSubmit → parent closes modal
User clicks Cancel → onCancel → parent closes modal

---

### 4.8 SnackbarComponent

Component Type:
Shared

Mapped From A1:
SnackbarComponent

Purpose:
Transient notification for errors/success

Parent:
KanbanBoardPage

Children:
- None

Dependencies:
- Snackbar.pdf

Library Components Used:
- Snackbar

TypeScript Specification
Inputs:
- message: string
- type: 'success' | 'error' | 'info'
- duration: number
Outputs:
- onDismiss()
State:
- None
Methods:
- handleDismiss()

HTML STRUCTURE (PSEUDO CODE ONLY)
<Snackbar type={type} duration={duration} onDismiss={onDismiss}>
  {message}
</Snackbar>

CSS SPECIFICATION
Layout:
- position: fixed; bottom: 24px; left: 50%; transform: translateX(-50%);
Spacing:
- padding: 16px;
Component Styles:
- .snackbar.error { background: #d32f2f; color: #fff; }
- .snackbar.success { background: #388e3c; color: #fff; }
Responsive:
- @media (max-width: 768px) { width: 90vw; }

API INTEGRATION
- None

ERROR HANDLING
- None (shows error messages)

INTERACTION FLOW
Auto-dismiss after duration or user clicks dismiss

---

### 4.9 PopupComponent

Component Type:
Shared

Mapped From A1:
PopupComponent

Purpose:
Confirmation, guided setup, support popups

Parent:
KanbanBoardPage

Children:
- None

Dependencies:
- Popup.pdf

Library Components Used:
- Popup

TypeScript Specification
Inputs:
- type: 'confirmation' | 'guided-setup' | 'error' | 'support'
- message: string
Outputs:
- onConfirm()
- onCancel()
State:
- None
Methods:
- handleConfirm()
- handleCancel()

HTML STRUCTURE (PSEUDO CODE ONLY)
<Popup type={type}>
  <Message>{message}</Message>
  <Button onClick={onConfirm}>Confirm</Button>
  <Button onClick={onCancel}>Cancel</Button>
</Popup>

CSS SPECIFICATION
Layout:
- position: fixed; top: 40vh; left: 50%; transform: translateX(-50%, -50%);
Spacing:
- padding: 24px;
Component Styles:
- .popup.confirmation { border-left: 4px solid #1976d2; }
Responsive:
- @media (max-width: 768px) { width: 90vw; }

API INTEGRATION
- None

ERROR HANDLING
- None (shows error messages)

INTERACTION FLOW
User clicks Confirm → onConfirm → parent handles action
User clicks Cancel → onCancel → parent closes popup

---

5. COMPONENT MAPPING SUMMARY

| A1 Component      | Final Component         | Type    |
|-------------------|------------------------|---------|
| Header            | HeaderComponent        | Layout  |
| Sidebar           | SidebarComponent       | Layout  |
| KanbanBoard       | KanbanBoardComponent   | Feature |
| KanbanColumn      | KanbanColumnComponent  | Feature |
| KanbanCard        | KanbanCardComponent    | Feature |
| AddTaskButton     | AddTaskButtonComponent | Feature |
| Modal             | ModalComponent         | Shared  |
| Snackbar          | SnackbarComponent      | Shared  |
| Popup             | PopupComponent         | Shared  |

6. DATA FLOW (FROM AGENT-1)

Parent → Child:
KanbanBoardPage
→ HeaderComponent (searchText, user)
→ SidebarComponent (menuItems, selected)
→ KanbanBoardComponent (columns)
  → KanbanColumnComponent (column)
    → KanbanCardComponent (card)
    → AddTaskButtonComponent (columnId)
→ ModalComponent (isOpen, mode, card)
→ SnackbarComponent (message, type, duration)
→ PopupComponent (type, message)

7. API USAGE SUMMARY

Component → API:
- KanbanBoardComponent → GET /api/youth-accounts/{youthAccountId}/dashboard
- ModalComponent (Task Creation/Edit) → (No direct API, recommend POST/PUT /tasks)

8. NOTES & CONSTRAINTS

- HTML must follow A1 structure strictly
- Do NOT create new components unnecessarily
- Prefer reusable library components
- HTML must be pseudo-code ONLY (NOT real framework code)
- ARIA attributes must be present for accessibility
- Responsive breakpoints must be implemented as per Jira and HTML
- Error handling must surface errors in Snackbar or Popup as appropriate
- All interactive elements must be keyboard accessible
- API for Kanban CRUD is missing; recommend extension
