## UI COMPONENT ARCHITECTURE

1. JIRA REQUIREMENT SUMMARY

Story Description:
AS a parent or guardian I WANT to allocate and manage funds for my child’s youth account SO THAT I can teach financial responsibility while maintaining visibility and control over spending.

Acceptance Criteria:
- Scenario 1 – Access youth account dashboard: Given the parent is logged into the banking web application, when the parent navigates to the youth account section, then the system should display the youth account dashboard with current balance and recent activity.
- Scenario 2 – Allocate funds to youth account: Given the parent is on the youth account dashboard, when the parent selects “Add Funds”, then the system should allow the parent to transfer funds from their primary account to the youth account.
- Scenario 3 – Set spending limit: Given the parent is viewing the youth account management section, when the parent configures a weekly spending limit, then the system should save and display the limit on the dashboard.
- Scenario 4 – View youth spending activity: Given the parent is viewing the youth account dashboard, when the parent selects the activity section, then the system should display recent transactions made from the youth account.
- Scenario 5 – Handle insufficient balance during fund allocation: Given the parent attempts to transfer funds, when the selected parent account does not have sufficient balance, then the system should display an error message indicating insufficient funds.

UI Tasks:
- Design youth account dashboard layout
- Design fund transfer interaction for youth account
- Design spending limit configuration interface
- Design youth account activity view

2. HTML STRUCTURE SUMMARY (SOURCE OF TRUTH)

Header:
- Application header with search field, notification icon, and parent avatar.

Sidebar / Navigation:
- Sidebar with navigation links: Dashboard, Add Funds, Set Limit, Activity, and error/confirmation popups.
- Footer with version and copyright.

Main Container:
- Page content area with conditional rendering for each section (dashboard, transfer, limit, activity, errors, confirmations).

Sections:
- Cards: Account Balance, Spending Limit, Recent Activity, Financial Literacy Tips
- Forms: Fund Transfer (source, amount, templates, transfer limit), Set Spending Limit (current, new, guidance)
- Lists: Recent Activity, Transaction List
- Buttons: Add Funds, Set Limit, View Activity, Transfer, Save, Edit, Cancel, Retry, Confirm
- Modals/Popups: Transfer Confirmation, Transfer Success, Limit Update Confirmation, Error Snackbars

3. LAYOUT STRUCTURE (MANDATORY)

Page Layout Hierarchy:

<YouthAccountPage>
├── Header
├── Main Layout
│   ├── Sidebar
│   └── Content Area
│       ├── DashboardSection
│       ├── FundTransferSection
│       ├── SpendingLimitSection
│       ├── ActivitySection
│       ├── ConfirmationModals
│       └── ErrorSnackbars

Layout Type:
- Mixed (Sidebar: Flex, Main Content: Grid/Flex, Cards: Grid)

4. REUSABLE COMPONENTS IDENTIFIED

From Library (ZIP):
- Header
- Sidebar
- Card
- Button
- Modal
- Snackbar
- Timeline
- Avatar
- Chip-Tag
- Divider
- Grid
- Tabs
- Dialog

5. UI COMPONENT ARCHITECTURE

<YouthAccountPage>
├── HeaderComponent
├── SidebarComponent
├── MainContainer
│   ├── DashboardSection
│   │   ├── Card (AccountBalanceCard)
│   │   ├── Card (SpendingLimitCard)
│   │   ├── Card (RecentActivityCard)
│   │   ├── Card (FinancialTipsCard)
│   │   └── ButtonGroup (AddFunds, SetLimit, ViewActivity)
│   ├── FundTransferSection
│   │   ├── Modal (FundTransferModal)
│   │   │   ├── Form (SourceAccountSelect, AmountInput, Templates, TransferLimitBadge)
│   │   │   └── ButtonGroup (Transfer, Cancel)
│   ├── SpendingLimitSection
│   │   ├── Modal (SpendingLimitModal)
│   │   │   ├── InfoRow (CurrentLimit)
│   │   │   ├── Input (NewLimitInput)
│   │   │   ├── InfoRow (Guidance)
│   │   │   └── ButtonGroup (Save, Cancel)
│   ├── ActivitySection
│   │   ├── Card (TransactionListCard)
│   ├── ConfirmationModals
│   │   ├── Modal (TransferConfirmationModal)
│   │   ├── Modal (TransferSuccessModal)
│   │   ├── Modal (LimitUpdateConfirmationModal)
│   └── ErrorSnackbars
│       ├── Snackbar (DashboardLoadError)
│       ├── Snackbar (InsufficientBalanceError)
│       ├── Snackbar (InvalidLimitError)

6. COMPONENT DEFINITIONS

Component Name: HeaderComponent
Purpose:
- Displays application logo, search, notification, and parent avatar.
Props:
- user: object
- onSearch: function
State:
- searchText: string
Events:
- searchChange
API Binding:
- None

Component Name: SidebarComponent
Purpose:
- Navigation for dashboard, fund transfer, limit, activity, errors, confirmations.
Props:
- navItems: array
- selected: string
Events:
- navSelect
API Binding:
- None

Component Name: AccountBalanceCard
Purpose:
- Shows youth account balance and last updated date.
Props:
- balance: number
- currency: string
- lastUpdated: string
API Binding:
- GET /api/youth-accounts/{youthAccountId}/dashboard

Component Name: SpendingLimitCard
Purpose:
- Shows current spending limit and allows edit.
Props:
- limit: number
- currency: string
- onEdit: function
API Binding:
- GET /api/youth-accounts/{youthAccountId}/dashboard

Component Name: RecentActivityCard
Purpose:
- Shows recent transactions (date, merchant, amount, balance).
Props:
- transactions: array
API Binding:
- GET /api/youth-accounts/{youthAccountId}/dashboard

Component Name: FinancialTipsCard
Purpose:
- Shows financial literacy tips.
Props:
- tips: array
API Binding:
- None

Component Name: ButtonGroup
Purpose:
- Renders grouped action buttons.
Props:
- actions: array
Events:
- onClick
API Binding:
- None

Component Name: FundTransferModal
Purpose:
- Modal for transferring funds to youth account.
Props:
- sourceAccounts: array
- selectedSource: string
- amount: number
- templates: array
- transferLimit: number
- currency: string
State:
- formState: {source, amount}
Events:
- onTransfer
- onCancel
API Binding:
- POST /api/youth-accounts/{youthAccountId}/fund-transfer

Component Name: SpendingLimitModal
Purpose:
- Modal for setting/updating weekly spending limit.
Props:
- currentLimit: number
- newLimit: number
- guidance: string
- currency: string
State:
- formState: {newLimit}
Events:
- onSave
- onCancel
API Binding:
- PUT /api/youth-accounts/{youthAccountId}/spending-limit

Component Name: TransactionListCard
Purpose:
- Shows full transaction list with date, merchant, amount, balance.
Props:
- transactions: array
API Binding:
- GET /api/youth-accounts/{youthAccountId}/transactions

Component Name: TransferConfirmationModal
Purpose:
- Modal to confirm fund transfer details.
Props:
- sourceAccount: string
- amount: number
- recipient: string
Events:
- onConfirm
- onBack
API Binding:
- POST /api/youth-accounts/{youthAccountId}/fund-transfer

Component Name: TransferSuccessModal
Purpose:
- Modal to show transfer success and updated balance.
Props:
- updatedBalance: number
- currency: string
Events:
- onDashboard
- onViewActivity
API Binding:
- None

Component Name: LimitUpdateConfirmationModal
Purpose:
- Modal to show limit update success.
Props:
- newLimit: number
- currency: string
Events:
- onDashboard
API Binding:
- None

Component Name: Snackbar (DashboardLoadError, InsufficientBalanceError, InvalidLimitError)
Purpose:
- Shows error messages for dashboard load, insufficient balance, invalid limit.
Props:
- message: string
- type: string
Events:
- onRetry
- onDashboard
API Binding:
- None

7. DATA FLOW / PROPS

Parent → Child Mapping:
<YouthAccountPage>
→ HeaderComponent (user, onSearch)
→ SidebarComponent (navItems, selected)
→ MainContainer
   → DashboardSection (balance, limit, transactions, tips)
   → FundTransferSection (sourceAccounts, templates, transferLimit)
   → SpendingLimitSection (currentLimit, guidance)
   → ActivitySection (transactions)
   → ConfirmationModals (transfer details, updated balance, new limit)
   → ErrorSnackbars (error messages)

8. API SUMMARY

Available APIs:
- GET /api/youth-accounts/{youthAccountId}/dashboard → Fetch dashboard data (balance, limit, recent activity)
- POST /api/youth-accounts/{youthAccountId}/fund-transfer → Transfer funds to youth account
- PUT /api/youth-accounts/{youthAccountId}/spending-limit → Update weekly spending limit
- GET /api/youth-accounts/{youthAccountId}/transactions → List youth account transactions

9. API TO UI MAPPING

Component → API:
- AccountBalanceCard, SpendingLimitCard, RecentActivityCard → GET /dashboard
- FundTransferModal, TransferConfirmationModal → POST /fund-transfer
- SpendingLimitModal → PUT /spending-limit
- TransactionListCard → GET /transactions

10. API COMPATIBILITY

Compatible:
- All UI requirements are supported by available APIs (dashboard, fund transfer, spending limit, transactions).
Missing:
- None
Transformations:
- UI fields map directly to API models (e.g., amount, sourceAccountId, weeklyLimit, transactions).

11. JIRA vs HTML VALIDATION

Matching Elements:
- Dashboard cards (balance, limit, recent activity)
- Fund transfer modal/page
- Spending limit modal/page
- Activity/transaction list
- Error and confirmation modals/snackbars
Missing Elements:
- None
Extra Elements:
- Financial tips card (not explicitly in Jira, but non-blocking)

12. FOLDER STRUCTURE (MANDATORY)

src/
 ├── components/
 │    ├── layout/
 │    │    ├── HeaderComponent
 │    │    ├── SidebarComponent
 │    │
 │    ├── feature/
 │    │    ├── AccountBalanceCard
 │    │    ├── SpendingLimitCard
 │    │    ├── RecentActivityCard
 │    │    ├── FinancialTipsCard
 │    │    ├── FundTransferModal
 │    │    ├── SpendingLimitModal
 │    │    ├── TransactionListCard
 │    │    ├── TransferConfirmationModal
 │    │    ├── TransferSuccessModal
 │    │    ├── LimitUpdateConfirmationModal
 │    │
 │    ├── shared/
 │         ├── Button
 │         ├── Card
 │         ├── Modal
 │         ├── Snackbar
 │         ├── Timeline
 │         ├── Avatar
 │         ├── Chip-Tag
 │         ├── Divider
 │         ├── Grid
 │         ├── Tabs
 │         ├── Dialog
 │
 ├── pages/
 │    ├── YouthAccountPage/
 │         ├── YouthAccountPage.tsx

13. INTERACTION FLOWS

User Action → UI → API
- Click "Add Funds"
  → Open FundTransferModal
  → Submit Transfer
  → Call POST /fund-transfer
  → Show TransferConfirmationModal
  → On success, show TransferSuccessModal
- Click "Set Limit"
  → Open SpendingLimitModal
  → Submit new limit
  → Call PUT /spending-limit
  → Show LimitUpdateConfirmationModal
- Click "View Activity"
  → Show ActivitySection
  → Call GET /transactions
- Error (e.g., insufficient balance)
  → Show Snackbar (InsufficientBalanceError)
- Dashboard load error
  → Show Snackbar (DashboardLoadError)

14. GAPS & RECOMMENDATIONS

Missing UI:
- None (all Jira requirements are covered)
Improvements:
- Add loading indicators for async API calls
- Add pagination/filtering to transaction list if needed
Performance:
- Use OnPush change detection for all feature components
- Lazy load modals and transaction list for performance
- Reuse shared components from library wherever possible
